package com.example.apptest.repository;

import com.example.apptest.model.Greeting;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;

import java.util.Arrays;

import static org.junit.jupiter.api.Assertions.*;

class GreetingRepositoryTest {

   /* @Autowired
    private GreetingRepository repository;

    Greeting greeting = new Greeting();
    Greeting greeting2 = new Greeting();
    //save, findById, findAll, deleteById

    @BeforeAll
    public void init() {
        greeting.setContent("Hello, Alex");
        greeting2.setContent("Hello, Kate");
        repository.saveAll(Arrays.asList())
    }

    @Test
    void deleteGreetingById() {
        Greeting deletedGreeting = repository.deleteGreetingById()
    }*/
}